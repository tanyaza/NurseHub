import React from 'react';
import CardContainer from '../../containers/CardContainer';
import MainContainer from '../../containers/MainContainer';

const Home = function () {
    return (
        <MainContainer title="خدمات">
            <CardContainer />
        </MainContainer>
    )
};

export default Home;
